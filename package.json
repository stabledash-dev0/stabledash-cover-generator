{
  "name": "stabledash-cover-generator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start -p $PORT",
    "lint": "next lint",
    "postinstall": "npx puppeteer browsers install chrome --path=/opt/render/.cache/puppeteer && echo 'Chrome installed to /opt/render/.cache/puppeteer' || echo 'Chrome installation failed, will use system Chrome'"
  },
  "dependencies": {
    "next": "14.0.0",
    "node-fetch": "^3.3.2",
    "puppeteer": "^24.25.0",
    "react": "^18",
    "react-dom": "^18",
    "sharp": "^0.32.6"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.0.0",
    "typescript": "^5"
  }
}
